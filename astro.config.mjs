// @ts-check
import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  site: "https://alvessss-design.github.io",
  devToolbar: { enabled: false },
});
